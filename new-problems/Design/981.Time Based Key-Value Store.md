# 981. Time Based Key-Value Store

**LeetCode:** {https://leetcode.com/problems/time-based-key-value-store/description/?envType=company&envId=apple&favoriteSlug=apple-three-months}  
**YoutubeLink** {https://www.youtube.com/watch?v=fu2cD_6E8Hw}
**Date:** {2026-03-01}  
**Status:** ❌ Stuck / 🔁 Redo  
**Difficulty:** Medium  
**Tags/Pattern:** {design, strings, hash table}  
**Time/Space:** O(log n) / O(n) -> set is O(1), get is O(log n)

---

## 1) Problem in 1 line

- Design a data structure that stores values for a key at different timestamps and returns the most recent value whose timestamp is less than or equal to a given time.
  Explain the problem in one sentence.

---

## 2) My first thought (before coding)

- no clue
  What I tried first, and why.

---

## 3) Mistakes / Confusions (Error Notebook 핵심)

- What I learned:

* I learned that when data is stored in sorted order, binary search becomes a powerful tool for fast lookups.
* I understood how to design a hashmap where each key maps to a list of timestamped values.
* I realized that many “design” problems are about combining simple data structures in a smart way.

---

## 4) Final approach (in plain English)

- Keep a hashmap where each key points to a list of pairs like `[value, timestamp]`, stored in the order they are added.
- When you call `set`, just append the new `[value, timestamp]` pair to that key’s list.
- When you call `get`, look up the list for that key; if it doesn’t exist, return `""`.
- Use binary search on the timestamps to find the **rightmost** pair whose timestamp is `<=` the given timestamp.
- While searching, whenever you find a valid timestamp, save its value as the current best answer and move right to try to find a closer one.
- Return the saved value at the end (or `""` if none of the timestamps were valid).

Steps of the solution like you’d explain to a friend.

---

## 5) Code (final)

```python
class TimeMap:

    def __init__(self):
        self.store={} #key=string, value=[list of [value, timestamp]]

    def set(self, key: str, value: str, timestamp: int) -> None:
        if key not in self.store:
            self.store[key]=[]
        self.store[key].append([value, timestamp])

    def get(self, key: str, timestamp: int) -> str:
        res= "" #if key doesnt exist in the store itself
        values=self.store.get(key, []) #return value for they key, if not key resturn []

        #binary search
        l,r =0, len(values)-1
        while l<=r:
            m=(l+r)//2
            if values[m][1]<=timestamp:
                #oh we need the value not the timestamp
                res=values[m][0] #setting the closest
                l=m+1
            else:
                r = m-1
        return res

# Your TimeMap object will be instantiated and called as such:
# obj = TimeMap()
# obj.set(key,value,timestamp)
# param_2 = obj.get(key,timestamp)
# paste final accepted code here
```
