# 733. Flood Fill

**LeetCode:** {https://leetcode.com/problems/flood-fill/description/?envType=company&envId=apple&favoriteSlug=apple-three-months}  
**Date:** {2026-01-17}  
**Status:** ❌ Stuck / 🔁 Redo  
**Difficulty:** Medium
**Tags/Pattern:** {arrays, matrix}  
**Time/Space:** O(mxn) / O(mxn)

---

## 1) Problem in 1 line

- Starting from a given pixel, change its color and recursively recolor all connected (up, down, left, right) pixels that have the same original color.
  Explain the problem in one sentence.

---

## 2) My first thought (before coding)

- i did not put efforts into thinking, knew a glimpse of dfs to be used and matrix traversal
  What I tried first, and why.

---

## 3) Mistakes / Confusions (Error Notebook 핵심)

- What I learned:
- try coding before giving up

---

## 4) Final approach (in plain English)

- First, remember the original color of the starting pixel.
- If the original color is already equal to the new color, just return (nothing to change).
- Use DFS (or BFS) to explore the grid starting from (sr, sc).
- For each cell, only continue if it’s inside the grid and matches the original color.
- Change its color, then repeat the process for its 4 neighbors (up, down, left, right).
  Steps of the solution like you’d explain to a friend.

---

## 5) Code (final)

```python
class Solution:
    def floodFill(self, image: List[List[int]], sr: int, sc: int, color: int) -> List[List[int]]:
        rows, cols=len(image), len(image[0])
        original_color=image[sr][sc]

        if original_color==color:
            return image
        def dfs(r,c):
            if r<0 or r>=rows or c<0 or c>=cols:
                return
            #we only explore and change color of pixels same as original color
            if original_color!=image[r][c]:
                return

            image[r][c]=color

            dfs(r+1,c)
            dfs(r-1,c)
            dfs(r,c+1)
            dfs(r,c-1)
        dfs(sr,sc)
        return image
# paste final accepted code here
```
