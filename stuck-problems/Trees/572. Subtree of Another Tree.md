# 572. Subtree of Another Tree

**LeetCode:** {https://leetcode.com/problems/subtree-of-another-tree/description/}  
**Date:** {2026-01-26}  
**Status:** ❌ Stuck / 🔁 Redo  
**Difficulty:** Easy  
**Tags/Pattern:** {Trees}  
**Time/Space:** O(N+M) / O(logN+logM)

---

## 1) Problem in 1 line

check if subroot tree has same values and structure in root tree
Explain the problem in one sentence.

---

## 2) My first thought (before coding)

recursion so thought of using while loop, thought of edge cases of if one exists and others doesnt
What I tried first, and why.

---

## 3) Mistakes / Confusions (Error Notebook 핵심)

- Mistake #1: recursion so function should be used
- Mistake #2: function in a function usage not known. if mismatch in same tree then subtree calls recursion towards left or right
- What I learned:
- every function in recursion has return inside the function
- recursion with functions
- edge cases testing properly

---

## 4) Final approach (in plain English)

- if not root return false
- write a function to check subtree exists
- inside the above function test edge cases, if values not same return false leading to subtree doing its recursion
- call subtree function as return with left traversal or right traversal

Steps of the solution like you’d explain to a friend.

---

## 5) Code (final)

```python
# Definition for a binary tree node.
# class TreeNode:
#     def __init__(self, val=0, left=None, right=None):
#         self.val = val
#         self.left = left
#         self.right = right
class Solution:
    def isSubtree(self, root, subRoot):
        if not root:
            return False
        def isSameTree(p,q):
            if not p and not q:
                return True
            if not p or not q:
                return False
            if p.val!=q.val:
                return False
            #if this returns false dive more deep into actual trees
            return isSameTree(p.left,q.left) and isSameTree(p.right,q.right)
        if isSameTree(root,subRoot):
            return True
        return self.isSubtree(root.left,subRoot) or self.isSubtree(root.right, subRoot)

# paste final accepted code here
```
