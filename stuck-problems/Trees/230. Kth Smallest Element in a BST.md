# 230. Kth Smallest Element in a BST

**LeetCode:** {https://leetcode.com/problems/kth-smallest-element-in-a-bst/description/}  
**Date:** {2026-01-28}  
**Status:** ❌ Stuck / 🔁 Redo  
**Difficulty:** Medium
**Tags/Pattern:** {trees}  
**Time/Space:** O(h+k) / O(h) h=height of the tree, k=nodes visited

---

## 1) Problem in 1 line

find the kth smallest element in the tree
Explain the problem in one sentence.

---

## 2) My first thought (before coding)

thought should use depth and do left traversal first for the smallest element and use a helper function (node,depth)
What I tried first, and why.

---

## 3) Mistakes / Confusions (Error Notebook 핵심)

- Mistake #1: not knowing to use count and result variable using self.
- Mistake #2: base case of not node or result is not None to return the final answer
- What I learned:
- base case and return importance

---

## 4) Final approach (in plain English)

- take variables self.result=None, self.count=0
- write a helper function with node
- base case if not node or result is not None return
- search(node.left) to traverse left subtree and increment count in next line
- if count==result then assign result as node.val and return
- write recursive of right sub tree
- call the helper function
- return result in the main function
  Steps of the solution like you’d explain to a friend.

---

## 5) Code (final)

```python
# Definition for a binary tree node.
# class TreeNode:
#     def __init__(self, val=0, left=None, right=None):
#         self.val = val
#         self.left = left
#         self.right = right
class Solution:
    def kthSmallest(self, root: Optional[TreeNode], k: int) -> int:
        self.count=0
        self.result=None
        def search(node):
            if not node or self.result is not None:
                return
            search(node.left)
            self.count+=1
            if self.count==k:
                self.result=node.val
                return
            search(node.right)
        search(root)
        return self.result
# paste final accepted code here
```
