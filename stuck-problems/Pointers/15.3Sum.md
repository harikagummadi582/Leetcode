# 15. 3Sum

**LeetCode:** https://leetcode.com/problems/3sum/description/
**Date:** {2026-01-19}  
**Status:** ❌ Stuck / 🔁 Redo - almost solved
**Difficulty:** Medium
**Tags/Pattern:** two pointers
**Time/Space:** O(n^2) / O(n^2)

---

## 1) Problem in 1 line

should find triplets where sum of their elements is zero; avoid duplicates
Explain the problem in one sentence.

---

## 2) My first thought (before coding)

to use pointers, sort the input, use while inside for to avoid duplicates, use extra list to store the result
What I tried first, and why.

---

## 3) Mistakes / Confusions (Error Notebook 핵심)

- Mistake #1: ignored duplicate nums[i]
- Mistake #2: ignored curr_sum beign less or more than 0 scenarios; nums[i]>0 then break; nums[i]==nums[i-1] then continue
- Mistake #3: wrote while loops outside if curr_sum==0
- What I learned:
  sort an array with output new list = nums.sorted(nums)
  take curr_sum ==0, <0, >0 scenarios
  consider duplicates in ith iterations to avoid them

---

## 4) Final approach (in plain English)

sort the array
for loop for traversing
in for loop consider duplicates scenarios of ith iteration and >0 ith number scenario
while loop and assign left=i+1, right=len(nums)-1
in while loop if curr_sum==0 then while again to ignore duplicates
curr_sum<0 then left+=1
currsum>0 then right-=1
return res
Steps of the solution like you’d explain to a friend.

---

## 5) Code (final)

```python
class Solution:
    def threeSum(self, nums: List[int]) -> List[List[int]]:
        nums=sorted(nums)
        res=[]
        for i in range(len(nums)):
            if nums[i]>0:
                break
            if i>0 and nums[i]==nums[i-1]:
                continue
            left=i+1
            right=len(nums)-1

            while left<right:
                curr_sum= nums[i]+nums[left]+nums[right]
                if curr_sum==0:
                    res.append([nums[i],nums[left],nums[right]])
                    while left<right and nums[left]==nums[left+1]:
                        left+=1
                    while left<right and nums[right]==nums[right-1]:
                        right-=1
                    left+=1
                    right-=1
                elif curr_sum<0:
                    left+=1
                else:
                    right-=1
        return res
# paste final accepted code here
```
