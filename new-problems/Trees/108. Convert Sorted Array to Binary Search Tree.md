# 108. Convert Sorted Array to Binary Search Tree

**LeetCode:** {https://leetcode.com/problems/convert-sorted-array-to-binary-search-tree/description/?envType=company&envId=apple&favoriteSlug=apple-three-months}  
**Date:** {2026-01-17}  
**Status:** ❌ Stuck / 🔁 Redo  
**Difficulty:** Easy
**Tags/Pattern:** {trees}  
**Time/Space:** O(n)- each node created once so n nodes / O(log n) - creating height balanced recursion depth is about log n

---

## 1) Problem in 1 line

- creating a height balanced binary search tree from sorted array given as an input
  Explain the problem in one sentence.

---

## 2) My first thought (before coding)

- have two helper functions one for left side of the treee and other right side of the tree both having input as the root. discovered middle element should be the root based on even or odd length input array
  What I tried first, and why.

---

## 3) Mistakes / Confusions (Error Notebook 핵심)

- Mistake #1: TreeNode is node used to create the node directly considered root.val, node.val
- Mistake #2: did not think of pointers to find middle element as the root.
- What I learned:
- pointers for finding the middle element helps not having to consider length of the array even or odd
- TreeNode(value) to be utilised to build a tree
- return root and return the helper function to return the root once traversal is done

---

## 4) Final approach (in plain English)

- Use two pointers, left and right, to represent the current subarray boundaries.
- Find the middle index mid = (left + right) // 2.
- Create a TreeNode using nums[mid] — this becomes the root of the current subtree.
- Recursively build the left subtree using the left half (left to mid - 1) and assign it to root.left.
- Recursively build the right subtree using the right half (mid + 1 to right) and assign it to root.right.
- Stop when left > right (base case), which means there are no elements left to create a node.

Steps of the solution like you’d explain to a friend.

---

## 5) Code (final)

```python
# Definition for a binary tree node.
# class TreeNode:
#     def __init__(self, val=0, left=None, right=None):
#         self.val = val
#         self.left = left
#         self.right = right
class Solution:
    def sortedArrayToBST(self, nums: List[int]) -> Optional[TreeNode]:
        def helper(left,right):
            if left>right:
                return None
            p=(left+right)//2
            root=TreeNode(nums[p])
            root.left=helper(left,p-1)
            root.right=helper(p+1,right)
            return root
        return helper(0, len(nums)-1)
# paste final accepted code here
```
