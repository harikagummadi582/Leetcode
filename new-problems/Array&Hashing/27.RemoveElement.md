# {27}. {Remove Element}

**LeetCode:** {https://leetcode.com/problems/remove-element/description/?envType=study-plan-v2&envId=top-interview-150}  
**Date:** {2026-09-21}  
**Status:** ✅ Solved  
**Difficulty:** Easy  
**Tags/Pattern:** {Arrays}  
**Time/Space:** O(n) / O(1)

---

## 1) Problem in 1 line

numbers same as val mentioned in the problem to be removed from the array and the array should have remaining values in any order but without spaces in the array

---

## 2) My first thought (before coding)

i thought of just having a count and counting the numbers present that are not equal to val in the array and return it but the array to be structured as mentioned in the question did not cross

---

## 3) Mistakes / Confusions (Error Notebook 핵심)

- Mistake #1: i thought of just having a count and counting the numbers present that are not equal to val in the array and return it but the array to be structured as mentioned in the question did not cross
- Mistake #2:
- What I learned: i learned to add print statements to debug and know where i am going wrong

---

## 4) Final approach (in plain English)

- take a variable lets say i
- write for loop to traverse through the array
- lets say if the number is not equal to value then array[i]=array[index]
- then i+=1 this way we count the values not equal to val and the array is structured accordingly
- return i

---

## 5) Code (final)

```python
# paste final accepted code here
class Solution:
    def removeElement(self, nums: List[int], val: int) -> int:
        i=0
        for k in range(len(nums)):
            if nums[k]!=val:
                nums[i]=nums[k]
                i+=1
        return i
```
